import matchers from "@testing-library/jest-dom/matchers";
import { expect } from 'vitest'

expect.extend(matchers)

//mock elements (localstorage)

// setup your  test server worker

//mock function  timers | dom elements

